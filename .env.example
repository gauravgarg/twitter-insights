# Required
BEARER_TOKEN=your_x_api_bearer_token

# Optional (comma-separated usernames without @)
TWITTER_HANDLES=finGuru,moneyTalks

# Optional: collector sleep seconds (default 3600 = 1 hour)
COLLECTOR_INTERVAL=3600
